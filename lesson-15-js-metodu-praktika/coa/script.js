//! funkcijos ir metodai
//? Task 1

function minToSec(number) {
  const fn = (n1) => n1 * 60 + ' seconds';
  return fn(1);
}

// console.log(minToSec());

//? Task 2

function manYear(number) {
  const fn = (n1) => n1 * 365;
  return fn(23);
}

// console.log(manYear());

//? Task 3

function square(number) {
  const fn = (n1) => n1 * n1;
  return fn(4);
}

console.log(square());
